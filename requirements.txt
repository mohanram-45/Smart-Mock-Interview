# Core ML
numpy>=1.24.0
pandas>=2.1.0

# NLP & Interview
sentence-transformers>=2.2.0
torch>=2.0.0

# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0

# PDF Generation
reportlab>=4.0.0

# Utilities
python-dotenv>=1.0.0
python-docx>=0.8.11

# Grammar checking for the TF-IDF + Grammar scoring model (requires a
# local Java runtime - see note below)
language-tool-python>=2.7.1

# Local LLM report narrative (optional - falls back to rule-based text
# automatically if Ollama isn't installed/running, see llm_report.py)
ollama>=0.4.0